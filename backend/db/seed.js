// Running `npm run seed` simply requires db/index.js, which seeds
// the default admin account and sample menu items if the tables are empty.
require("./index");
console.log("Database is ready and seeded (if it was empty).");
