# Build Log — Cafe/Restaurant Online Ordering Platform

> Fill this in with your own words before submitting. This is a starting skeleton, not a final answer.

## What I built
Briefly describe the app in your own words: what a customer can do, what an admin can do,
and the overall architecture (React frontend, Express + SQLite backend, JWT auth).

## Tech stack
- Frontend: React, Vite, React Router
- Backend: Node.js, Express, better-sqlite3, JWT, bcrypt
- Deployment: (fill in — e.g. Vercel for frontend, Render for backend)

## Which AI tools I used, and how
Be specific and honest, e.g.:
- Used Claude to scaffold the initial project structure and generate the Express routes / React pages.
- Reviewed and tested every endpoint manually before accepting it.
- Wrote/adjusted [specific parts] myself — e.g. changed the color palette, added an extra field, fixed a bug in X.
- Used AI to explain [concept] I wasn't familiar with (e.g. JWT auth, SQLite transactions).

## What I learned
- e.g. How JWT-based auth works end-to-end (issuing, verifying, protecting routes).
- e.g. How to structure a REST API around resources (menu, orders, auth).
- e.g. How React context (useContext) can manage global state like cart/auth without prop drilling.
- e.g. Trade-offs of SQLite vs. a hosted database, and free-tier deployment constraints.

## Challenges & how I solved them
- e.g. Description of a bug you hit and how you debugged it.

## What I'd improve with more time
- e.g. Payment integration (Stripe test mode), image uploads for menu items, email notifications on status change, pagination for orders, tests.
